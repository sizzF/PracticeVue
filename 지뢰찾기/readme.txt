Vuex쓰기전에 컴포넌트 여러개로 나눈 이유가 td하나만 변경되면 그것만 변경하기위해서였는데

Vuex를 쓰면 tableData변경될때마다 TdComponent에 컴퓨티드가 전체 다바뀌면서 td9개가 다 업데이트됨
(사실 실제로 데이터가 변한건 아니라 화면까지 바꾼건아니지만....
버츄얼돔을 쓰기때문에 데이터에 차이가 없으면 버츄얼돔에서 화면이 안바꼈다고 생각해서 안바꿈)

그래서 Vuex쓸때는 이렇게까지 나눌 필요가없고 그냥 TicTacToe에 직접 렌더링해서 처리함


TableComponent에 slot으로 새로운 내용 넣어주는 부분도 있음 확인

slot을 왜쓸까
렌더링은 TableComponent에서 되지만 정작 데이터 가져와서 처리하는부분은 TicTacToe에서 하고있음
어 그럼 TableComponent에서 막 데이터 가져오고 하는짓을 안해도되네?
자식 컴포넌트에서 표시되어야할 내용을 부모컨퍼넌트에다 적어줌으로써 부모컨퍼넌트의 데이터나 컴퓨티드같은거에 접근할수 있다.
관리를 부모가한다
inversion of control(IOC)

v-for 에 key값에 배열인덱스써도 되는경우는 배열의 값이 중간에 삭제되지않는경우
왜냐하면 [1,2,3,4]이렇게 있는데 1을지워서 [2,3,4]이렇게되면 배열의 인덱스값이 다바뀌고 이러면 key값이 바뀐걸로 인식돼서 1만 지우면 되는걸
2,3,4를 다 다시그려버림

키정하는게 참 애매해 그냥 math.random()이런거 써서 고유한값을 잘 만들어주자